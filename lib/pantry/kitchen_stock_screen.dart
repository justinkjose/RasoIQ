import 'package:flutter/material.dart';

class KitchenStockScreen extends StatelessWidget {
  const KitchenStockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Kitchen Stock Screen'),
    );
  }
}
